version: '3'
services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    extends:
      file: application.yaml
      service: db

volumes:
  postgres-data:
  db: